import { Layout } from "@/components/Layout";
import { Journal } from "@/components/Journal";

const Index = () => {
  return (
    <Layout>
      <Journal />
    </Layout>
  );
};

export default Index;